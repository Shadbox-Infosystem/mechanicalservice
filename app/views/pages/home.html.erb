<!-- Hero Slider -->
<div class="swiper hero-slider mb-0 position-relative hero-slider-fullwidth" style="min-height: 520px; display: flex; align-items: center; justify-content: center;">
  <div class="swiper-wrapper">
    <% @sliders.each do |slider| %>
      <div class="swiper-slide position-relative" style="min-height: 520px; display: flex; align-items: center; justify-content: center;">
        <% if slider.image.attached? %>
          <%= image_tag slider.image, style: 'object-fit: cover; width: 100vw; height: 520px; position: absolute; left: 0; top: 0; z-index: 1;', alt: slider.title %>
        <% else %>
          <div style="background: linear-gradient(90deg, #1e3c72 0%, #2a5298 50%, #6a11cb 100%); width: 100vw; height: 520px; position: absolute; left: 0; top: 0; z-index: 1;"></div>
        <% end %>
        <!-- Overlay for readability -->
        <div style="position: absolute; left: 0; top: 0; width: 100vw; height: 520px; background: rgba(20,24,47,0.45); z-index: 2;"></div>
        <div class="carousel-caption animate__animated animate__fadeInDown text-center slider-caption-content" style="position: relative; z-index: 3; width: 100vw; display: flex; flex-direction: column; align-items: center; justify-content: center;">
          <h1 style="font-size: 3.2rem; font-weight: 800; color: #fff; line-height: 1.1; text-shadow: 0 4px 24px rgba(0,0,0,0.35); max-width: 700px; margin: 0 auto 1.2rem auto; padding: 0 1rem;">
            <%= slider.title %>
          </h1>
          <% if slider.description.present? %>
            <p style="font-size: 1.35rem; color: #e0e0e0; margin-bottom: 2.5rem; text-shadow: 0 2px 12px rgba(0,0,0,0.25); max-width: 600px; margin-left: auto; margin-right: auto; padding: 0 1rem;">
              <%= slider.description %>
            </p>
          <% end %>
        </div>
      </div>
    <% end %>
  </div>
</div>

<div class="section-divider"></div>

<!-- Products Section -->
<section class="products-section py-5" style="background: #f6f8fa;">
  <div class="container">
    <h2 class="text-center mb-4" style="color: #1e3c72; font-weight: 800; letter-spacing: 1px;">Our Products</h2>
    <div class="swiper products-slider">
      <div class="swiper-wrapper">
        <% @products&.each do |product| %>
          <div class="swiper-slide">
            <div class="card h-100 product-card shadow border-0 rounded-4">
              <% if product.image.attached? %>
                <%= image_tag product.image, class: "card-img-top", alt: product.name %>
              <% else %>
                <img src="https://via.placeholder.com/300x200" class="card-img-top" alt="<%= product.name %>">
              <% end %>
              <div class="card-body">
                <h5 class="card-title" style="color: #1e3c72; font-weight: 700; letter-spacing: 0.5px;"><%= product.name %></h5>
                <p class="card-text"><%= truncate(product.description, length: 100) %></p>
                <%= link_to "Learn More", product_path(product), class: "btn btn-primary w-100" %>
              </div>
            </div>
          </div>
        <% end %>
      </div>
      <div class="swiper-pagination"></div>
      <div class="swiper-button-next"></div>
      <div class="swiper-button-prev"></div>
    </div>
    <div class="text-center mt-4">
      <%= link_to "View All Products", products_path, class: "btn btn-outline-primary px-4 py-2" %>
    </div>
  </div>
</section>

<div class="section-divider"></div>

<!-- Gallery Section -->
<section class="gallery-section py-5" style="background: #fff;">
  <div class="container">
    <h2 class="text-center mb-4 gallery-heading-gradient">Our Gallery</h2>
    <div class="swiper gallery-slider">
      <div class="swiper-wrapper">
        <% @galleries&.each do |gallery| %>
          <% if gallery.images.attached? %>
            <% gallery.images.each do |image| %>
              <div class="swiper-slide">
                <div class="gallery-card shadow rounded-4 overflow-hidden">
                  <%= image_tag image.variant(resize_to_limit: [800, 600]), 
                              class: "img-fluid rounded", 
                              alt: gallery.title || "Gallery Image" %>
                  <div class="gallery-caption text-white text-center py-2" style="background: linear-gradient(90deg, #1e3c72 0%, #6a11cb 100%); font-weight: 600;">
                    <%= gallery.title %>
                  </div>
                </div>
              </div>
            <% end %>
          <% end %>
        <% end %>
      </div>
      <div class="swiper-pagination"></div>
      <div class="swiper-button-next"></div>
      <div class="swiper-button-prev"></div>
    </div>
    <div class="text-center mt-4">
      <%= link_to "View Full Gallery", galleries_path, class: "btn btn-outline-primary px-4 py-2" %>
    </div>
  </div>
</section>

<style>
  .hero-slider {
    min-height: 520px;
    width: 100vw;
    left: 50%;
    right: 50%;
    margin-left: -50vw;
    margin-right: -50vw;
    max-width: 100vw;
    border-radius: 0;
    padding: 0;
    background: linear-gradient(90deg, #1e3c72 0%, #2a5298 50%, #6a11cb 100%) !important;
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .hero-slider-fullwidth {
    width: 100vw !important;
    margin-left: calc(-50vw + 50%);
    margin-right: calc(-50vw + 50%);
    left: 0 !important;
    right: 0 !important;
    border-radius: 0 !important;
    padding: 0 !important;
  }
  .carousel-caption h1 {
    font-size: 3.2rem;
    font-weight: 800;
    color: #fff;
    line-height: 1.1;
    margin-bottom: 1.2rem;
  }
  .carousel-caption p {
    font-size: 1.35rem;
    color: #e0e0e0;
    margin-bottom: 2.5rem;
  }
  .btn-warning {
    background: #ffd600 !important;
    color: #181c2f !important;
    border: none !important;
    font-weight: 700;
    font-size: 1.1rem;
    box-shadow: 0 2px 8px rgba(255,214,0,0.08);
    border-radius: 2rem;
    transition: background 0.2s, color 0.2s;
  }
  .btn-warning:hover {
    background: #ffe082 !important;
    color: #181c2f !important;
  }
  .btn-outline-light {
    border: 2px solid #fff !important;
    color: #fff !important;
    font-weight: 700;
    font-size: 1.1rem;
    border-radius: 2rem;
    transition: background 0.2s, color 0.2s;
  }
  .btn-outline-light:hover {
    background: #fff !important;
    color: #181c2f !important;
  }
  .products-section {
    background: #f6f8fa;
  }
  .product-card {
    transition: box-shadow 0.3s, transform 0.3s;
    border-radius: 1.5rem;
    box-shadow: 0 2px 12px rgba(30,60,114,0.07);
  }
  .product-card:hover {
    box-shadow: 0 8px 32px rgba(106,17,203,0.13);
    transform: translateY(-6px) scale(1.03);
  }
  .gallery-card {
    background: #fff;
    border-radius: 1.5rem;
    overflow: hidden;
    transition: box-shadow 0.3s, transform 0.3s;
    box-shadow: 0 2px 12px rgba(106,17,203,0.07);
  }
  .gallery-card:hover {
    box-shadow: 0 8px 32px rgba(30,60,114,0.13);
    transform: translateY(-6px) scale(1.03);
  }
  .gallery-caption {
    font-weight: 500;
    font-size: 1rem;
    background: linear-gradient(90deg, #1e3c72 0%, #6a11cb 100%);
    opacity: 0.95;
  }
  .gallery-heading-gradient {
    font-weight: 800;
    letter-spacing: 1px;
    font-size: 2.2rem;
    background: linear-gradient(90deg, #1e3c72 0%, #6a11cb 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    text-fill-color: transparent;
  }
  .swiper-button-next,
  .swiper-button-prev {
    color: #007bff;
  }
  .swiper-pagination-bullet-active {
    background: #007bff;
  }
  .btn-primary, .btn-outline-primary {
    border-radius: 2rem;
    font-weight: 500;
    letter-spacing: 0.5px;
    box-shadow: 0 2px 8px rgba(30,60,114,0.08);
    transition: background 0.2s, color 0.2s;
    background: linear-gradient(90deg, #1e3c72 0%, #6a11cb 100%);
    border: none;
  }
  .btn-primary:hover, .btn-outline-primary:hover {
    background: linear-gradient(90deg, #6a11cb 0%, #1e3c72 100%);
    color: #fff;
    border-color: #6a11cb;
  }
  .section-divider {
    border: 0;
    height: 2px;
    background: linear-gradient(90deg, #1e3c72 0%, #6a11cb 100%);
    opacity: 0.10;
    margin: 0 0 2rem 0;
  }
  body, h2, h5, p {
    font-family: 'Segoe UI', 'Roboto', Arial, sans-serif;
  }
</style>

<!-- Animate.css CDN for text animation -->
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" />

<script>
  document.addEventListener('DOMContentLoaded', function() {
    // Hero Slider
    new Swiper('.hero-slider', {
      loop: true,
      autoplay: {
        delay: 5000,
      },
      pagination: {
        el: '.hero-slider .swiper-pagination',
        clickable: true,
      },
      navigation: {
        nextEl: '.hero-slider .swiper-button-next',
        prevEl: '.hero-slider .swiper-button-prev',
      },
    });

    // Products Slider
    new Swiper('.products-slider', {
      slidesPerView: 1,
      spaceBetween: 20,
      loop: true,
      pagination: {
        el: '.products-slider .swiper-pagination',
        clickable: true,
      },
      navigation: {
        nextEl: '.products-slider .swiper-button-next',
        prevEl: '.products-slider .swiper-button-prev',
      },
      breakpoints: {
        640: {
          slidesPerView: 2,
        },
        768: {
          slidesPerView: 3,
        },
        1024: {
          slidesPerView: 4,
        },
      },
    });

    // Gallery Slider
    new Swiper('.gallery-slider', {
      slidesPerView: 1,
      spaceBetween: 20,
      loop: true,
      pagination: {
        el: '.gallery-slider .swiper-pagination',
        clickable: true,
      },
      navigation: {
        nextEl: '.gallery-slider .swiper-button-next',
        prevEl: '.gallery-slider .swiper-button-prev',
      },
      breakpoints: {
        640: {
          slidesPerView: 2,
        },
        768: {
          slidesPerView: 3,
        },
        1024: {
          slidesPerView: 4,
        },
      },
    });
  });
</script> 